{% extends '@VinorcolaPrivateUser/layout.html.twig' %}

{% block pageTitle %}{{ 'private_user.admin.activationLink.title'|trans({ '%user%': user.displayName }) }}{% endblock %}
{% block title %}{{ 'private_user.admin.activationLink.title'|trans({ '%user%': user.displayName }) }}{% endblock %}



{% block private_user_body %}
    {% set link = url('private_user.registration.register', { token: user.token }) %}

    <p>{{ 'private_user.admin.activationLink.message'|trans({ '%expirationDate%': expirationDate }) }}</p>

    <pre>{{ link }}</pre>

    <p><a href="{{ path('private_user.admin.list') }}">{{ 'private_user.admin.activationLink.back'|trans }}</a></p>
{% endblock %}
