{% extends '@VinorcolaPrivateUser/layout.html.twig' %}

{% block pageTitle %}{{ 'private_user.admin.edit.title'|trans({ '%user%': user.displayName }) }}{% endblock %}
{% block title %}{{ 'private_user.admin.edit.title'|trans({ '%user%': user.displayName }) }}{% endblock %}



{% block private_user_body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="button-group">
            <button type="submit" class="btn btn-primary">{{ 'private_user.admin.edit.submit'|trans }}</button>
            <a href="{{ path('private_user.admin.list') }}">{{ 'private_user.admin.edit.cancel'|trans }}</a>
        </div>
    {{ form_end(form) }}
{% endblock %}
